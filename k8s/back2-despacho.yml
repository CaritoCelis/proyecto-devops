apiVersion: apps/v1
kind: Deployment
metadata:
  name: back2-despacho
  labels:
    app: back2-despacho
spec:
  replicas: 1
  selector:
    matchLabels:
      app: back2-despacho
  template:
    metadata:
      labels:
        app: back2-despacho
    spec:
      containers:
        - name: back2-despacho
          image: AWS_ACCOUNT_ID.dkr.ecr.us-east-1.amazonaws.com/back2-despacho:latest
          ports:
            - containerPort: 8080
          env:
            - name: DB_USERNAME
              value: "root"
            - name: DB_PASSWORD
              value: "rootpassword"
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:mysql://mysql-despacho:3306/devops_despacho?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&createDatabaseIfNotExist=true"
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "500m"
              memory: "1Gi"
---
apiVersion: v1
kind: Service
metadata:
  name: back2-despacho
spec:
  selector:
    app: back2-despacho
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
---
# Base de datos MySQL para despacho
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-despacho
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-despacho
  template:
    metadata:
      labels:
        app: mysql-despacho
    spec:
      containers:
        - name: mysql-despacho
          image: mysql:8
          ports:
            - containerPort: 3306
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: "rootpassword"
            - name: MYSQL_DATABASE
              value: "devops_despacho"
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "500m"
              memory: "1Gi"
---
apiVersion: v1
kind: Service
metadata:
  name: mysql-despacho
spec:
  selector:
    app: mysql-despacho
  ports:
    - port: 3306
      targetPort: 3306
  type: ClusterIP